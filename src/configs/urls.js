const baseURL = 'http://owu.linkpc.net/api/v3';

const urls = {
    cars: '/cars',
    users: '/users',
    auth: {
        login: '/auth',
        refresh: '/auth/refresh'
    }
}
export {baseURL, urls}