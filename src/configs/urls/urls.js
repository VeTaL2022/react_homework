const baseURL = "https://jsonplaceholder.typicode.com";

const todoURL = {
    todos: "/todos"
}
const albumURL = {
    albums: "/albums"
}
const commentURL = {
    comments: "/comments"
}
const postURL = {
    posts: "/posts"
}
export {baseURL,todoURL,albumURL,commentURL,postURL}